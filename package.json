{
    "name": "rivulet",
    "version": "1.0.0",
    "description": "Virtual HDHomeRun tuner for Plex with IPTV M3U parsing and transcoding",
    "main": "backend/app.js",
    "scripts": {
      "build:ui": "cd frontend && npm install && npm run build && rm -rf ../backend/public && cp -r build ../backend/public",
      "start": "node backend/app.js"
    },
    "dependencies": {
      "better-sqlite3": "^8.5.2",
      "express": "^4.18.2",
      "ffmpeg-static": "^5.0.1",
      "node-fetch": "2.6.7"
    },
    "engines": {
      "node": ">=14.0.0"
    }
  }
  